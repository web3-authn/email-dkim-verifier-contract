# NEAR account ID where the EmailDkimVerifier contract will be deployed.
CONTRACT_ID=your-contract.testnet
NEAR_NETWORK_ID=testnet

# Keys for the deployer account (used by deploy/upgrade scripts)
DEPLOYER_PUBLIC_KEY=ed25519:
DEPLOYER_PRIVATE_KEY=ed25519:

# Account ID that will sign request_email_verification calls
SIGNER_ID=your-account.testnet

# Optional: pin the Outlayer worker WASM URL (e.g. https://outlayer.tatchi.xyz/workers/email-dkim/<sha>.wasm).
# If unset, scripts fetch the latest build manifest:
# https://outlayer.tatchi.xyz/workers/email-dkim/latest.json
OUTLAYER_WORKER_WASM_URL=

# Optional: override the "latest" manifest URL (used when OUTLAYER_WORKER_WASM_URL is unset).
OUTLAYER_WORKER_WASM_LATEST_MANIFEST_URL=
